---
name: session-fix-sweep-2026-07
description: July 2026 sweep fixed all GAP_ANALYSIS items — role provisioning built, all three logins verified working
metadata:
  type: project
---

On 2026-07-04 nearly every GAP_ANALYSIS.md finding was fixed in one sweep (~135 files). Key durable facts:

- **Logins**: the root cause of "only partner works" was that `signUp` never set a role and `mapSupabaseUser` returns null without one — AND the documented `admin@jtacs.test` auth user didn't exist in Supabase at all. Now: signup=PARTNER; Add Employee provisions a real Supabase login via [[lib/auth/provisioning.ts]] (invite email + one-time temp password fallback, `must_change_password` metadata forces `/auth/set-password` on first login, enforced in proxy.ts). Test accounts created: admin@jtacs.test / JTacs@Admin2026! (PARTNER), manager@jtacs.test / Manager@2026!, employee@jtacs.test / Employee@2026! — all three verified end-to-end in the browser.
- **Select/options fix**: `color-scheme: dark` on `.dark` + global `select option` styling in globals.css + `bg-popover` on Radix SelectContent (was 2% transparent white).
- **Payment details** (bank/UPI) live on FirmSettings (columns added via `prisma db push`) → Settings form → client-portal Pay dialog + invoice PDFs (`/api/invoices/[id]/pdf`, client-ownership checked).
- **Conventions established**: themed `components/ui/confirm-dialog.tsx` replaces window.confirm/alert; submit buttons must never hard-disable behind silent `canSubmit` gates (disable only while pending); `?new=1`/`?upload=1`/`?compose=1` deep-link params open create dialogs on clients, work-tracker, invoices, documents, messaging, compliance; route-level `loading.tsx` uses `components/layout/route-loading.tsx`.
- Messaging is now honestly **email-only** (relabeled per user decision); content is escaped + wrapped via `lib/messaging/email-html.ts`. WhatsApp API code kept in lib/messaging for future use.
- Onboarding wizard is PARTNER-only (managers hit a server-rejected firm-settings step before).
- **Billing is service-based, not e-commerce** (user directive): no Quantity anywhere. Invoices carry serviceDescription/serviceType/professionalFee/taxRate/taxAmount/remarks (columns added via db push; `amount` remains the canonical total = fee + GST). Quotation line items are Service Description + Professional Fee + GST% (`quantity` column kept but always 1). Shared client-ready document: `components/proposals/quotation-document.tsx` (letterhead, fee/GST table, amount-in-words via `lib/quotations/terms.ts`, T&C, signature blocks) — used by builder live preview, internal detail, and public portal; keep all three in sync through it.
- **pdfkit must stay in `serverExternalPackages`** (next.config.ts) — bundling breaks its AFM font resolution (ENOENT Helvetica.afm) on every PDF route.
- Dates rendered in client components must pass an explicit locale (`toLocaleDateString("en-IN")`) — locale-less calls hydration-mismatch against the server.
- After `prisma db push`, restart the dev server — it holds the old generated client in memory (Unknown argument errors otherwise).
- **Enterprise audit (2026-07-05, ENTERPRISE_AUDIT.md)**: live RBAC testing confirmed page-level proxy enforcement is solid for all roles. Found + FIXED: 🔴 Critical IDOR — `/api/invoices/[id]/pdf` allowed any staff incl. EMPLOYEE to download any invoice PDF (now PARTNER/MANAGER + CLIENT-owner only; re-verified employee gets 403); 🟠 High — Manager could disable/delete/ban another Manager (added `targetRoleGuard` in employees.ts delete/disable/enable — MANAGER-target is Partner-only); stale `Client.assignedEmployeeName` on employee rename (now synced); `getInvoicesData` hardened to requirePartnerOrManager; added Role column to Employees table. STILL OPEN (documented, not fixed): `must_change_password` skipped for /api + server actions (enforce in shared guard); Client-portal identity via non-unique email findFirst; no real MFA (`lib/security/mfa.ts` is dead code); no SSO; no audit-log viewer UI; Client-360 stale after invoice/task mutations (need revalidatePath(/clients/[id])); no server-side pagination/virtualization on tasks/invoices/documents/messages; no bulk ops/import; orphaned update actions (invoice/lead/document edit have no UI); no Partner staff-password-reset; single-tenant FirmSettings singleton.
- **Production-hardening pass (2026-07-05)** fixed most remaining audit items: `must_change_password` now enforced in `requireAuth` (SessionInfo.mustChangePassword flag) so it can't be skipped via /api/actions; Client-360 revalidation added to all invoice + task mutations; `updateInvoice` hardened (can't set PAID/PARTIALLY_PAID or paidAmount — ledger via recordPayment is authoritative); compliance completedAt now cleared on reopen (deriveStatusFromWorkflow returns completedAt:null) + updateComplianceEventStatus clears it for non-completed; client-portal identity rejects ambiguous email matches (findMany take:2); date validation on quotation validUntil + compliance dueDate; rejectionReason capped 1000; deleted dead lib/security/{audit-log,rate-limit}.ts; a11y: TableHead scope="col", FormField aria-describedby+role=alert, notification-bell Escape/focus-restore/role=dialog, prefers-reduced-motion in globals.css, StatusBadge/-workforce contrast bumped to -300/-400, quotation-document slate-400→500; Notifications added to Partner+Manager sidebars; client upload router.refresh() + fixed formats banner; removed dead employees bulk-selection UI; **Partner/Manager can now Reset a staff password** (resetStaffPassword in provisioning + resetEmployeePassword action w/ targetRoleGuard + one-time reveal dialog in employees UI); getClient360Data take:500 caps; search activityLog write made non-blocking; fixed duplicate PageHeader on Employees page. STILL DEFERRED (documented, need real infra/large work): SSO, real MFA, audit-log viewer UI, multi-tenancy, Redis rate-limiter, full server-side list pagination + virtualization, bulk ops, invoice/lead/document edit UIs, globalSearch parallelization. Build+tsc+lint all clean; IDOR re-verified 403 live.
- **Improvement roadmap** produced in `IMPROVEMENTS.md` (prioritized checklist, ~200 items grouped by category) — the durable backlog for "best CA-firm CRM" goal.
- **Implementation Wave 1 (2026-07-05)** — shipped from that roadmap, all verified (tsc + prod build + browser): Lead edit UI (`components/proposals/edit-lead-dialog.tsx` → `updateLead`); DRAFT-only Invoice edit (`components/payments/edit-invoice-dialog.tsx`; `updateInvoice` extended — full financial edit gated on `status==="DRAFT" && formData has serviceDescription`, else unchanged status-only path; SENT invoices stay immutable, clientId/invoiceNumber never change); Document metadata edit (`components/documents/edit-document-dialog.tsx`); **global role-aware "＋ New" header menu** in `dashboard-header.tsx` (uses `useAuth().role` + `canAccessRoute`, replaced the single "New Filing" button); proposals `?new=1`/`?tab=` deep-linking; clients **Export now respects active filters/sort** (moved into `clients-table.tsx` toolbar with CSV formula-injection guard + BOM; removed the header "Export all"); notification **deep-links** (`lib/notifications/entity-link.ts` maps entityType/entityId → route; card click navigates + marks read); Settings placeholder cleanup (deleted fake Billing card; SMS toggle → disabled "Coming soon"; "Push"→"In-App Notifications"). **Also fixed a real reliability bug**: `getLeads` + `getQuotations` returned raw Prisma `Decimal`s across the RSC boundary (console "Only plain objects" errors) — now serialized like `getQuotationById`/`getLeadDetail`. NOTE: editing a server action requires a **dev-server restart** — HMR served stale `getLeads` code and the Decimal errors persisted until restart; the preview MCP console buffer is cumulative (console.clear doesn't flush it), so restart to get a clean read.
- Work uncommitted at session end (user hadn't asked for a commit).
